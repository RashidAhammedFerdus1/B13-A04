1. What is the difference between getElementById, getElementsByClassName, and querySelector / querySelectorAll?
 #getElementById is usually select an element by it's unique id which is fast and but getElementsByclassname select an element by it's class. It can't declare a array directly. And querySelector is selects first matching element using CSS selector. 
2. How do you create and insert a new element into the DOM?
 #Usually create or insert a new element into the DOM means by using java script make a new html file and add it into the DOM by the method of appendChild, prepend etc. 
3. What is Event Bubbling? And how does it work?
 #Event Bubbling means if a event occur in a child, it will spread parents then grandparents then document. Means event climb from the lower element to upper element like bubble. 
4. What is Event Delegation in JavaScript? Why is it useful?
 #Event Delegation is a system where we don't use different event lisener in every child, we use a event listener to a parent element. It's call Event delegation. 
5. What is the difference between preventDefault() and stopPropagation() methods?
 #preventDefault() usually stop default browser behavior on the other hand stopPropagation() stop event bubbling, it stop bubbling to it's parent element.
